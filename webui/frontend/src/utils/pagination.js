// 全站列表统一的每页条数选项。邮箱列表、注册结果、候选管理共用同一套，
// 避免各页面各写一份导致用户在不同列表间切换时选项对不上。
export const PAGE_SIZE_OPTIONS = [20, 50, 100, 200, 500]

// 跨页全选时向后端一次性拉取全部匹配行的上限。当前最大的一张表（注册结果）
// 约 3k 行，留足冗余；真超过时按后端返回的 total 提示用户收窄筛选条件。
export const SELECT_ALL_FETCH_LIMIT = 100000
